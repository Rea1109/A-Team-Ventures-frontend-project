# 개발과제 제출\_우형래

```bash
-- 프로젝트 패키지 설치
npm install
or
yarn install

-- App 실행
yarn start

-- json-server port 는 3001로 설정
npm install -g json-server
json-server --watch ./src/db/requests.json --port 3001
```
